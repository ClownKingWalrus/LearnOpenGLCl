#version 330 core
out vec4 FragColor;
in vec3 ourColor;
in vec2 textCoord;
vec2 flipped = vec2(1.0 - textCoord.x, textCoord.y);

uniform sampler2D texture1;
uniform sampler2D texture2;

void main()
{
    FragColor = mix(texture(texture1, textCoord), texture(texture2, textCoord), 0.2);
}